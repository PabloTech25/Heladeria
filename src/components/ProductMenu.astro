---
import AddToCartButton from './AddToCartButton.svelte';
import type { Product, ProductCategory } from '../data/products';
interface Props {
  products: Product[];
}

const { products } = Astro.props;

// Get unique categories from products
const categories: ProductCategory[] = [
  "Helados",
  "Paletas de Hielo",
  "Bebidas Frias",
  "Bebidas Calientes",
  "Postres",
  "Galletas",
  "Salados",
  "Completos"
];
---

<section id="menu" class="py-24 bg-gradient-to-b from-white to-chaud-50">
  <div class="container mx-auto px-4">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <span class="inline-block px-4 py-2 bg-chaud-100 text-chaud-700 rounded-full text-sm font-semibold mb-4">
        Nuestras Especialidades
      </span>
      <h2 class="text-4xl md:text-5xl font-bold text-warm-900 mb-4">
        Menú Artesanal
      </h2>
      <p class="text-warm-600 text-lg max-w-2xl mx-auto leading-relaxed">
        Cada producto es elaborado al momento con ingredientes frescos y de la más alta calidad
      </p>
    </div>

    <!-- Category Filters -->
    <div class="mb-12">
      <div class="flex flex-wrap justify-center gap-3">
        <button
          data-category="all"
          class="category-filter active px-6 py-3 rounded-full font-semibold transition-all duration-300 bg-chaud-600 text-white shadow-lg hover:shadow-xl hover:scale-105"
        >
          Todos los productos
        </button>
        {categories.map((category) => (
          <button
            data-category={category}
            class="category-filter px-6 py-3 rounded-full font-semibold transition-all duration-300 bg-white text-warm-700 border-2 border-warm-200 hover:border-chaud-400 hover:text-chaud-600 hover:scale-105"
          >
            {category}
          </button>
        ))}
      </div>
    </div>

    <!-- Products Grid -->
    <div id="products-grid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {products.map((product) => (
        <div 
          class="product-card flex flex-col bg-white rounded-3xl shadow-lg overflow-hidden hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 group border border-chaud-100"
          data-category={product.category}
        >
          <!-- Product Image -->
          <div class="relative overflow-hidden h-64 bg-gradient-to-br from-chaud-50 to-chaud-100">
            <img 
              src={product.image} 
              alt={product.name}
              class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700"
              loading="lazy"
            />
            <!-- Price Badge -->
            <div class="absolute top-4 right-4 bg-chaud-600 text-white px-5 py-2.5 rounded-full font-bold shadow-xl backdrop-blur-sm z-10">
              {product.hasSizes && product.prices ? (
                <div class="flex flex-col items-center leading-tight">
                  <span class="text-[10px] uppercase tracking-widest opacity-80">Desde</span>
                  <span class="text-lg">{product.prices.chico.price}</span>
                </div>
              ) : (
                product.price
              )}
            </div>
          </div>
          
          <!-- Product Info -->
          <div class="p-6 flex-grow flex flex-col">
            <h3 class="text-2xl font-bold text-warm-900 mb-3 leading-tight">
              {product.name}
            </h3>
            
            <p class="text-warm-600 mb-4 leading-relaxed">
              {product.description}
            </p>
            
            {product.ingredients && (
              <div class="mb-3 p-3 bg-chaud-50 rounded-lg">
                <span class="text-sm font-bold text-chaud-700 block mb-1">
                  Ingredientes:
                </span>
                <p class="text-sm text-warm-700 leading-relaxed">
                  {product.ingredients}
                </p>
              </div>
            )}
            
            {product.hasSizes && product.prices ? (
              <div class="flex gap-3 mb-6">
                <div class="flex-1 p-3 bg-chaud-50 rounded-2xl border border-chaud-100 text-center">
                  <span class="block text-[10px] uppercase text-chaud-600 font-bold mb-1 tracking-wider">Chico</span>
                  <span class="font-bold text-warm-900 text-lg">{product.prices.chico.price}</span>
                </div>
                <div class="flex-1 p-3 bg-chaud-50 rounded-2xl border border-chaud-100 text-center">
                  <span class="block text-[10px] uppercase text-chaud-600 font-bold mb-1 tracking-wider">Grande</span>
                  <span class="font-bold text-warm-900 text-lg">{product.prices.grande.price}</span>
                </div>
              </div>
            ) : product.size && (
              <div class="mb-4">
                <span class="inline-flex items-center gap-2 text-sm font-semibold text-chaud-600 bg-chaud-50 px-3 py-1.5 rounded-full">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21a4 4 0 01-4-4V5a2 2 0 012 2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01"></path>
                  </svg>
                  {product.size}
                </span>
              </div>
            )}
            
            <div class="mt-auto">
              <AddToCartButton
                client:idle
                id={product.id}
                name={product.name}
                price={product.numericPrice}
                image={product.image}
                size={product.size}
              />
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  // Category filter functionality
  document.addEventListener('DOMContentLoaded', () => {
    const filterButtons = document.querySelectorAll('.category-filter');
    const productCards = document.querySelectorAll('.product-card');
    
    // Filter products by category
    function filterProducts(category: string) {
      productCards.forEach((card) => {
        const productCategory = card.getAttribute('data-category');
        
        if (category === 'all' || productCategory === category) {
          // Show card with fade-in animation
          card.classList.remove('hidden');
          card.classList.add('animate-fade-in-up');
        } else {
          // Hide card
          card.classList.add('hidden');
        }
      });
    }
    
    // Update active button state
    function updateActiveButton(activeButton: Element) {
      filterButtons.forEach((btn) => {
        btn.classList.remove('active', 'bg-chaud-600', 'text-white', 'shadow-lg');
        btn.classList.add('bg-white', 'text-warm-700', 'border-2', 'border-warm-200');
      });
      
      activeButton.classList.add('active', 'bg-chaud-600', 'text-white', 'shadow-lg');
      activeButton.classList.remove('bg-white', 'text-warm-700', 'border-2', 'border-warm-200');
    }
    
    // Add click event to filter buttons
    filterButtons.forEach((button) => {
      button.addEventListener('click', () => {
        const category = button.getAttribute('data-category') || 'all';
        filterProducts(category);
        updateActiveButton(button);
      });
    });
  });
</script>

<style>
  .product-card {
    animation: fade-in-up 0.5s ease-out;
  }
  
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .animate-fade-in-up {
    animation: fade-in-up 0.4s ease-out;
  }
</style>
